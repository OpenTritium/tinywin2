{
  "$schema": "../schemas/plan-v4.schema.json",
  "schemaVersion": 4,
  "id": "service.security-health",
  "version": "3.0.0",
  "title": "配置 Windows 安全中心服务启动方式",
  "description": "配置 SecurityHealthService（Defender 状态汇报 UI）的启动方式。disabled 仅用于离线 WIM；在线修改通常会被系统服务 ACL 拒绝。禁用后安全中心状态图标和健康汇报不可用，但不等于关闭 Defender 引擎。",
  "category": "Security",
  "riskLevel": "High",
  "operations": [
    {
      "resource": "registry.service",
      "action": "configure",
      "spec": {
        "services": [
          "SecurityHealthService"
        ],
        "start": {
          "$map": {
            "parameter": "startMode",
            "cases": {
              "disabled": "disabled",
              "manual": "manual",
              "delayedAuto": "DelayedAuto",
              "auto": "auto"
            }
          }
        }
      }
    }
  ],
  "parameters": [
    {
      "name": "startMode",
      "type": "enum",
      "label": "启动方式",
      "default": "delayedAuto",
      "options": [
        {
          "value": "delayedAuto",
          "label": "延迟启动",
          "riskLevel": "Medium"
        },
        {
          "value": "disabled",
          "label": "禁用（仅离线）",
          "riskLevel": "High"
        },
        {
          "value": "manual",
          "label": "手动",
          "riskLevel": "Medium"
        }
      ]
    }
  ]
}

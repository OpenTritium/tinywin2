{
  "$schema": "../schemas/plan-v5.schema.json",
  "schemaVersion": 5,
  "id": "feature.wsl",
  "version": "3.1.0",
  "title": "移除 WSL1 组件",
  "description": "移除 WSL1（Microsoft-Windows-Subsystem-Linux / lxcore）。WSL2 不受影响：其前置 VirtualMachinePlatform 由 feature.vm-platform 计划启用；WSL2 本体在部署后经 wsl --install --web-download 安装。",
  "category": "Virtualization",
  "riskLevel": "High",
  "operations": [
    {
      "resource": "dism.feature",
      "action": "remove",
      "spec": {
        "features": [
          "Microsoft-Windows-Subsystem-Linux"
        ],
        "removePayload": true
      }
    }
  ]
}
{
  "$schema": "../schemas/plan-v5.schema.json",
  "schemaVersion": 5,
  "id": "fs.catroot2",
  "version": "1.0.0",
  "title": "清理 catroot2 签名验证数据库",
  "description": "清空 CryptoAPI 的 catroot2 目录（目录数据库与日志）。CryptSvc 在离线镜像上不会重建；但实测部署后首次引导会把数据库重建回 ~150 MB（体量随 catroot 目录的签名目录集而定），因此本计划只缩小镜像产物，不缩小已部署系统的占用。catroot 目录内的 .cat 签名文件保留，不影响签名校验。",
  "category": "System",
  "riskLevel": "Medium",
  "operations": [
    {
      "resource": "fs.path",
      "action": "remove",
      "spec": {
        "paths": [
          "Windows\\System32\\catroot2\\*"
        ]
      }
    }
  ]
}

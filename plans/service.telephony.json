{
  "$schema": "../schemas/plan-v5.schema.json",
  "schemaVersion": 5,
  "id": "service.telephony",
  "version": "3.1.0",
  "title": "电话服务启动方式",
  "description": "TapiSrv：manual 为 Windows 默认（按需启动）。注意 RasMan 依赖 Telephony，禁用本服务会连带阻止 RasMan 启动，进而阻断开机离线阶段的网络组件注册（见 service.remote-access-connection-manager）。",
  "category": "Legacy",
  "riskLevel": "Low",
  "operations": [
    {
      "resource": "registry.service",
      "action": "apply",
      "spec": {
        "services": [
          "TapiSrv"
        ],
        "start": "manual"
      }
    }
  ]
}

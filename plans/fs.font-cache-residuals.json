{
  "$schema": "../schemas/plan-v5.schema.json",
  "schemaVersion": 5,
  "id": "fs.font-cache-residuals",
  "version": "1.1.0",
  "title": "清理字体缓存文件",
  "description": "删除镜像中已有的字体缓存（FNTCACHE.DAT）；配合禁用 FontCache 可释放约 19 MiB，系统会在需要时重新生成可用缓存。不含 StaticCache.dat：它由系统在每次引导时重建，且受 TrustedInstaller 保护无法离线删除，从镜像移除属于无效目标。",
  "category": "Servicing",
  "riskLevel": "Low",
  "operations": [
    {
      "resource": "fs.path",
      "action": "remove",
      "spec": {
        "paths": [
          "Windows\\System32\\FNTCACHE.DAT"
        ]
      }
    }
  ]
}

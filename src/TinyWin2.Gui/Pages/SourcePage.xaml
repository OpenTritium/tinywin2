<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TinyWin2.Gui.Pages.SourcePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Spacing="{StaticResource PageSpacing}" Padding="24,8,24,24" MaxWidth="860" HorizontalAlignment="Left">

            <TextBlock Text="选择安装源" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="SourceBox" PlaceholderText="D:\iso\server2025.iso 或媒体文件夹" Width="560" KeyDown="SourceBox_KeyDown"/>
                <Button Content="浏览 ISO…" Click="PickIso"/>
                <Button Content="浏览文件夹…" Click="PickFolder"/>
            </StackPanel>
            <ProgressRing x:Name="IndexSpinner" IsActive="False" Width="24" Height="24" HorizontalAlignment="Left"/>

            <TextBlock Text="镜像索引" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <ComboBox x:Name="IndexCombo" Width="560" DisplayMemberPath="Display" SelectionChanged="IndexSelected"
                      PlaceholderText="选择源后自动列出索引"/>

            <TextBlock Text="输出" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ComboBox x:Name="OutputModeCombo" Width="240" SelectionChanged="OutputModeSelected">
                    <ComboBoxItem Content="可引导 ISO (ESD)" Tag="iso" IsSelected="True"/>
                    <ComboBoxItem Content="可引导 ISO (ESD) + VHDX" Tag="iso+vhdx"/>
                    <ComboBoxItem Content="WIM 文件夹" Tag="wim"/>
                    <ComboBoxItem Content="ESD 文件夹" Tag="esd"/>
                </ComboBox>
                <ComboBox x:Name="GranularityCombo" Width="220" SelectionChanged="GranularitySelected" Header="分层粒度">
                    <ComboBoxItem Content="按大类一层（推荐）" Tag="group" IsSelected="True"/>
                    <ComboBoxItem Content="每条 plan 一层" Tag="plan"/>
                </ComboBox>
                <ToggleSwitch x:Name="FastToggle" Header="快速模式" OffContent="标准" OnContent="快速" Toggled="FastToggled"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="OutputBox" PlaceholderText="输出目录（默认 .\out）" Width="560" TextChanged="OutputChanged"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,16,0,0">
                <Button x:Name="NextButton" Content="下一步 →" Style="{ThemeResource AccentButtonStyle}" Click="GoNext"/>
                <TextBlock x:Name="HintText" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
